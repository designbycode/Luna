(function(){
  'use strict';

  $.fn.button = function(opt){
    var settings, $self = $(this);
    settings = $.extend({
    }, opt);

    return this.each(function(e){
      // $self.clone().prependTo( )
    });

  };

})();
